#include "main.h"
/**
 * main - Entry point of the program. It tests the function print_triangle
 * by printing triangles of various sizes.
 * Return: Always 0 (Success).
 */
void print_triangle(int size)
{print_triangle(2);
print_triangle(10);
print_triangle(1);
print_triangle(0);
return (0); }
